#include<stdio.h>
#include<math.h> //
int main()
{
	
		printf("%.f\n", pow(2, 5)); //2의 5승
		printf("%.f\n", sqrt(100)); //100 의 제곱근
	
	//pow값과 sqrt값을 변수에 저장하고 싶다면 double 자료형을 사용

	return 0;
}